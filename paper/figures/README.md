# Figure Source Files

Source files for the three figures in the manuscript, to be populated
before publication.

## Figure 1 — SMARTS Interaction Loop Schematic

**Status:** pending. To be produced as a clean vector flowchart
(TikZ source + rendered PDF). Content per the editor's decision:
Option A from the design discussion — showing student input → FRAME
check → SMARTS phase gate → response → feedback loop.

Will be delivered as:

- `figure1_smarts_loop.tex` (TikZ source)
- `figure1_smarts_loop.pdf` (rendered)

## Figure 2 — SMARTS Thinking-Partner Prompt, Part 1 (FRAME + SMARTS phases)

The in-paper rendering of Figure 2 is produced directly from the
`verbatim` environment in the manuscript `.tex` source. The canonical
text is in:

- `../../prompts/01_thinking_partner_bot.md`

## Figure 3 — SMARTS Thinking-Partner Prompt, Part 2 (Core rule + closing)

Same as Figure 2. The canonical text is in:

- `../../prompts/01_thinking_partner_bot.md`
